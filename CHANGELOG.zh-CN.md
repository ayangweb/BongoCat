# 更新日志

## Unreleased

- 本次版本把桌面应用完整重写为 Rust，替换原来的 WebView 版本，运行会更流畅，资源占用也会更少。

### ✨ 新功能

- 现在可以分别忽略键盘和手柄输入。新增的全局快捷键可以切换鼠标、键盘和手柄的忽略状态，默认不绑定按键。
- 自动检查更新的间隔可以按小时设置，从 1 小时到 8760 小时都可以；需要时仍可手动检查。
- 日志现在是带日期的普通文件，可以直接用文本编辑器打开。日志级别和保留时间都能设置，旧文件也会自动清理。
- 动作音效默认关闭；打开后，下一个动作就会发声，不需要重启。
- 按住鼠标右键就能调整模型窗口大小；普通右键仍然打开菜单。
- 导入模型时会先检查来源、显示进度并支持取消；封面会从模型实际渲染中生成。无法准备的模型会直接拒绝导入。
- 内置模型也可以改名、换封面，而且不会改动应用自带的文件。
- 导入 BongoCatMver 模型时可以直接转换，并选择要转换的模式；模型卡片会记住所选模式。
- 在“模型行为”页面设置间隔后，模型会定期随机播放一个动作或表情。
- F13–F24 现在可以使用专属图片，macOS 地球键也能单独绘制。
- 浅色和深色主题现在也会应用到支持的窗口、菜单和文件选择面板。
- 手柄摇杆和扳机可以设置死区。
- 快捷键分组各自有独立开关；模型快捷键在打开对应分组前不会生效。
- 设置现在按任务重新整理，模型库和模型行为都有独立页面。
- 应用启动时模型窗口会显示；隐藏只对当前会话有效。

### 🐛 问题修复

- 锁屏、睡眠、插拔设备或权限变化后，输入状态不会再卡住；macOS 的右 Shift、右 Alt 和 CapsLock 也更稳定。
- 左右 Alt、Enter、小键盘 Enter 和标准键盘现在会使用正确的键位图。
- 配置损坏时会自动恢复最新有效备份；没有有效备份时使用默认配置启动。
- BongoCatMver 转换窗口现在可以正常打开，不会闪退。
- 重新打开设置窗口会回到原来的页面和滚动位置。
- 动作播放完后会停在最后的姿态，直到被其他动作替换或被停止。

### ⚡️ 性能优化

- 最大帧率设置现在真的能达到你选择的数值。
- 设置窗口不再在后台反复重建全部内容；窗口关闭后也会停止刷新，省下 CPU。

### ⚠️ 升级说明

- 本版本使用新的设置和模型数据格式。旧版的设置、快捷键、已选模型和已安装模型不会自动带过来，升级后需要重新设置并重新导入模型。

### 💻 平台变化

- 支持 Windows 10 1903+（x64）和 macOS 12+（Intel/Apple 芯片）。
- Windows ARM 通过仿真运行 x64 版本，不提供 x86 或原生 ARM64 安装包。
- 本版本暂不支持 Linux。

### 🗑️ 移除或不再支持

- 模型卡片上的表情按钮已移除；动作和表情现在统一在“快捷键”页面查看。
- 繁体中文、葡萄牙语和越南语已移除，目前可选跟随系统、简体中文或英语。
- Windows 按键释放超时现在使用毫秒，不再使用秒数。
