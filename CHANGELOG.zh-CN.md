# 更新日志

## Unreleased

- 本次版本把桌面应用完整重写为 Rust，替换原来的 WebView 版本，运行会更流畅，资源占用也会更少。

### ⚠️ 升级说明

- 本版本使用全新的设置和模型数据格式。旧版设置、快捷键、模型选择和已安装模型不会自动导入；升级后需要重新配置并重新导入模型。模型窗口每次启动都会显示，隐藏只对当前会话有效。

### ✨ 新功能

- 现在可以分别忽略鼠标、键盘和手柄输入，为它们设置全局快捷键，并调整手柄死区。
- 导入模型时会检查文件夹、显示进度并支持取消；封面来自模型本身，无法生成封面时会撤销本次导入。
- 内置模型和导入模型都可以改名、换封面。BongoCatMver 文件夹也可以在导入时直接转换，并选择要使用的输入模式；每张模型卡片都会显示对应模式。
- 模型可以按设定间隔随机播放动作或表情。动作音效默认关闭，打开后无需重启即可生效。
- 模型窗口支持右键拖动缩放，缩放范围为 25–400%，透明度为 1–100%，跨屏移动时的位置调整也更顺畅。
- 自动检查更新可以设置为每 1–8760 小时执行一次；更新窗口会显示下载、校验、安装、重试和重启状态。
- 日志现在是可读的带日期文件，可以调整级别和保留时间，旧日志会自动清理。
- 设置按任务重新整理，浅色或深色主题也会应用到支持的系统窗口、菜单和文件选择面板。
- 窗口快捷键和模型行为快捷键现在各自拥有开关。模型快捷键默认关闭，按住组合键时也只会触发一次动作。

### 🐛 用户可见修复

- 设备变化、锁屏、睡眠和权限变化后，残留的按键和按钮状态现在会被清除；macOS 上的右 Shift、右 Option 和 Caps Lock 也更可靠。旧版仅限 Windows、按秒计时的超时现在改为可配置的毫秒兜底。
- 左右 Alt、Enter、小键盘 Enter，以及转换后模型的键名现在都能正确对应到键位图。
- 切换模型或修改尺寸、透明度时，模型窗口不会再短暂黑屏或完全透明。
- 触发的动作现在只播放一次，并保持最后姿态，直到被其他动作替换或被停止。
- 设置文件损坏时会自动使用最新的有效备份；没有有效备份时使用默认配置。
- 不完整或不合法的 v1 设置和模型 ID 现在会被一致地拒绝，不再部分接受或静默忽略。

### 💻 支持范围变化

- 支持 Windows 10 1903+（x64）和 macOS 12+（Intel/Apple 芯片）。Windows ARM 通过仿真运行 x64 版本，不再提供 x86、原生 ARM64 或 Linux 构建。
- 语言选项现在为跟随系统、简体中文和英语，不再提供繁体中文、葡萄牙语和越南语。
