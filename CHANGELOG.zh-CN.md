# 更新日志

[English](CHANGELOG.md) | [简体中文](CHANGELOG.zh-CN.md)

BongoCat 2.0.0 是本更新日志记录的首个版本。

## Unreleased

### 🐛 问题修复

- 已导入的模型现在可以在使用中直接删除。此前删除按钮会从正在使用的模型卡片上消失，想删掉它必须先切换到另一个模型。现在删除正在使用的模型会先切换到内置标准模型，再移除模型文件；切换失败时，模型和你的选择都保持原样。

## 2.0.0 - 2026-09-16

### ⚠️ 升级说明

- 本版本使用全新的配置和模型数据。旧版设置、快捷键、模型选择及已安装模型列表不会自动导入；升级后请重新设置，并重新导入模型。

### ✨ 新功能

- 新增持久化模型库，可导入模型文件夹或 ZIP 压缩包。导入时提供校验、进度和取消操作，不安全的压缩包会被拒绝。
- 可直接导入 BongoCatMver 模型。源中包含的每种输入模式（标准、键盘、手柄）都会转换成一个独立模型，并按来源名与模式命名，不再需要转换工具。
- 模型库以卡片展示每个模型自己的封面图和名称。在卡片上可以切换模型、打开模型所在位置、修改显示名称、更换封面图或删除模型，并会在启动时恢复上次使用的模型。模型切换失败时，当前可用模型不受影响。内置模型的名称和封面属于应用内容，不可修改；如需自定义请导入一份副本。
- 模型的动作和表情列在“快捷键”页面，可分别为每一项绑定全局快捷键。
- 模型的动作和表情会自动获得各自的全局快捷键，与旧版一致：加载模型后，每一项都会被填入 `Ctrl`/`Cmd` 加 `Shift`/`Alt` 与数字或字母的组合键，并且每一项仍可单独修改。改动过的绑定不会被覆盖，因此重新激活模型或恢复默认时只会补上你尚未录制的那部分。“快捷键”页面模型列表上方的“启用模型快捷键”开关默认关闭，在你打开它之前这些快捷键不会全局生效——旧版会无条件绑定，用户即使从未打开过行为列表，也已经背上一批全局快捷键；重新打开开关无需再次录制即可恢复。“清除全部快捷键”会永久移除窗口快捷键，但模型的默认快捷键会在该模型下次激活时重新补上——要阻止它们生效，保持该开关关闭即可。
- 改进更新流程，提供独立的更新窗口、下载进度、签名校验、Markdown 发行说明、安装和重启状态；更新失败时会保留当前版本并允许重试。
- 首次启动时会根据系统权限状态提供引导：macOS 可直接前往“输入监控”设置，Windows 会提示管理员权限对全局输入的影响。缺少输入权限时，模型仍会启动。
- 模型可为 F1–F24 提供 `F1.png` 至 `F24.png` 专属图片；缺少专属图片的功能键会回退到模型共享的 `Fn.png`，只提供 `Fn.png` 的模型行为与之前一致。
- macOS 左下角的 Fn（地球）键现在是一个独立键位，名称为 `Globe`，不再与功能键的共享图片同名。`Fn` 保持原义：模型可以为整个 F1–F24 行提供的那一张共享图片，因此 `Fn.png` 既不改名也不迁移。想绘制地球键的模型提供 `Globe.png` 即可，旧名 `Function.png` 仍然可用。Windows 上该键由键盘固件处理、系统不会上报，因此不产生任何反应。
- 浅色/深色外观选择现在覆盖操作系统绘制的原生表面：设置窗口标题栏在双平台都跟随主题；macOS 上的弹框（包括启动权限提示）、菜单、托盘菜单和文件选择面板同样跟随，模型窗口的右键菜单从首次启动起就是正确主题；Windows 的窗口边框跟随主题，弹框、菜单和文件选择框受系统限制继续跟随系统主题。

### ⚡️ 性能优化

- 优化模型显示，减少不必要的 CPU/GPU 占用，并改善缩放和连续显示时的流畅度。
- 支持 15–240 FPS 的帧率上限；模型隐藏时会降低刷新频率，减少后台资源占用。
- 应用不再在后台反复重建完整设置快照。系统菜单改为询问廉价的变化标记，而不是每秒重建快照二十次；关闭（预渲染）的设置窗口在重新显示前不再刷新。macOS 上这同时让设置循环不再重复查询系统权限，因此空闲时的 CPU 占用明显下降。

### 🗑️ 移除内容

- 移除了“诊断”页面。运行计数值、渲染与输入细节、构建标识、匿名诊断报告导出和备份目录入口都属于面向开发者的信息，而不是用户可以据此采取行动的内容，因此设置窗口现在只展示能改变 BongoCat 行为的页面。BongoCat 本来就写入的完整日志保持不变，仍是排障时的依据，匿名诊断报告也仍由应用自身的诊断工具生成。原本提示“配置无法读取”的说明和“恢复默认配置”按钮已移到“通用”页面，与它所描述的设置放在一起。

### 🐛 问题修复

- 优化键鼠状态校正和恢复流程，在锁屏、睡眠、设备变化或权限变化后自动重置，减少按键或鼠标卡住的情况。
- macOS 上右 Shift、右 Alt（Option）和 CapsLock 现在能够即时、可靠地响应。监听事件 tap 改为像 rdev 一样创建在 HID 层最前端：macOS 26 的 session 层 tap 收不到右 Shift 的释放事件，HID 层 tap 则能收到每个修饰键完整的按下/释放事件对。修饰键的按下/释放方向改为从每颗物理键独立的设备位跳变解码，不再依赖有歧义的家族 flag；CapsLock 的锁存式 flag 由边沿交替兜底，周期校正同时会查询右侧键码无法正确报告的家族主键码状态。
- 配置写入增加自动备份和损坏恢复，避免异常退出或配置损坏导致设置丢失。
- 左右 Alt 现在按两个不同的按键绘制。此前按下右侧 Alt 会显示左侧 Alt 的图片，模型自带的右侧 Alt 图片（`AltGr.png`）也不会生效。仍把两个键命名为 `Alt` 和 `AltGr` 的模型会在导入时自动转换为新名称，已安装的模型也会继续显示正确的图片。
- 主 Enter 与小键盘 Enter 现在按正确的名称绘制。主 Enter 的图片改用 `Enter` 命名（旧的 `Return` 命名会在导入时自动转换，已安装的模型也能继续显示图片），模型没有提供专属 `KpEnter.png` 时小键盘 Enter 会回退使用主 Enter 的图片；此前按下小键盘 Enter 不会显示任何内容。
- 小键盘按键现在会正常绘制，不再毫无反应。与主键盘重复的小键盘键——`Num 1` 至 `Num 9`、`Num 0`、小键盘 Enter 和小键盘 `/`——会回退使用对应主键盘按键的图片，因此只画了主键盘的模型也能响应小键盘。主键盘上没有对应键的五个小键盘键（`Num Lock`、`*`、`-`、`+`、`.`）仍然不绘制，因为从未有模型为它们提供图片。这也让上文的小键盘 Enter 回退首次真正生效。
- 标准键盘的每个按键现在都有名称，包括从未有模型画过的那些：标点键（`.`、`,`、`-`、`=`、`[`、`]`、`\`、`;`、`'`）、PrintScreen、Scroll Lock、Pause、导航键（Insert、Home、Page Up、Delete、End、Page Down）、ISO 额外键、Apps 以及小键盘 `=`。模型只要为其中任一按键提供图片即可生效，无需改动产品代码。Delete 键的图片一直随内置模型出厂，却始终无法绘制，现在可以正常显示。
- 只有在当前模型确实有对应图片时，按键才会产生反应。按下模型没有绘制图片的按键——例如内置模型没有 `Dot.png`，按下 `.` 时——此前会让爪子下压，但屏幕上不会出现任何按键。这类按键现在完全不执行任何动作：爪子不动，也不绘制按键图层，因此不会为看不到的东西产生反馈。提供对应图片的模型不受影响，包括回退到主键盘图片的小键盘按键、回退到 `Fn.png` 的功能键，以及内置模型的 Delete 键。
- 模型在 Windows 和 macOS 上的颜色现在一致。此前 Windows 的模型窗口整体偏暗、对比更强，因为渲染流程缺少最后一步颜色转换；纯黑和纯白不受影响，半透明边缘的权重也不正确。
- 转换 BongoCatMver 模型时，功能键 F13–F24 现在各自对应自己的图片。转换出的反斜杠键也会安装 `BackSlash.png`，即应用实际查找的那张图片；此前转换器写出的是 `Backslash.png`，没有任何按键能选中它，按下该键完全不产生反应。
- Apps 键（右 Ctrl 旁边的菜单键）现在在两个平台都能工作，提供 `Apps.png` 的模型可以画出它。该键从一开始就有名字和绑定，但两个平台都产不出它：Windows Raw Input 没有它的扫描码，macOS 没有 `kVK_ContextualMenu` 的键码，因此那张图片永远选不中。苹果键盘没有这个键，但第三方键盘会发送该 macOS 键码。
- 模型动作快捷键现在只播放一次动作，不再无限循环。内置动作本身声明为循环，此前触发后会一直播放，模型不会回到静止姿态；按住快捷键也不会再因按键重复而反复触发，动作播放结束后的下一次按下会重新播放。
- 主猫窗口现在每次启动都会重新显示。隐藏只在当前会话生效，退出前隐藏的窗口不会在下次启动后继续保持隐藏，与旧版行为一致。
- 设置窗口在 Windows 和 macOS 上的行为现在完全一致：关闭只隐藏窗口，再次打开显示的是同一个窗口，不再重新创建。窗口会停在你离开时的位置（同一页面、同一滚动位置），也不再有重建停顿，而 macOS 此前每次关闭再打开都要付出这段停顿。
- 模型动作和表情的快捷键现在对每个模型都从 `Ctrl`/`Cmd+1` 开始编号，并且只有你正在使用的模型占用它们。此前激活第二个模型会接着上一个模型的编号往后排——以内置模型为例，它的第一个动作会落在 `Ctrl`/`Cmd+8` 上——因为其它模型已经拿到的组合键被算作已占用。切换模型现在也立即生效：被离开的模型不再触发它独占的快捷键，新模型的快捷键无需重启即可使用——两个模型排到同一位置的组合键会改由新模型响应，此前它答的仍然是旧模型，新模型的快捷键因此要等到下次启动才会生效。不属于当前模型的快捷键也不再注册到系统，此前它们占用了永远不会触发的组合键。
- 模型动作和表情在快捷键页现在按位置命名，不再显示模型包内部的名称。内置模型原本显示 `standard (motion:CAT_motion:0)` 到 `standard (motion:CAT_motion_lock:1)`，现在显示“动作 1”到“动作 4”；表情原本显示 `standard (expression:live2d_expression0.exp3.json)`，现在显示“表情 1”到“表情 3”。编号会先跨所有动作组连续排完，再开始排表情，因此同一类里不会出现重复，动作的编号也与自动分配的快捷键所用数字一致。每个快捷键触发的动作或表情保持不变，读屏播报的名称也同步改用这套名称。
- 修改快捷键页上任意一个控件时，整页不再闪动。此前每次保存往返都会让两个分组（包括两个启用开关和全部录制行）整体变暗再恢复，看起来像整页刷新了一次；现在保存进行期间各行外观保持不变，页头状态栏继续承担“正在保存”的提示。

### 🎨 界面与体验

- 设置界面重新整理导航和状态反馈，补齐加载、空状态、错误与重试界面。
- 模型缩放范围调整为 25%–400%，透明度支持 1%–100%。
- 新增窗口圆角设置，范围为窗口宽高的 0%–50%：0% 为直角，50% 时窗口被裁剪为完整椭圆。
- 新增“鼠标悬停时隐藏”设置，可让模型窗口在鼠标停留时淡出并临时穿透，鼠标离开后恢复；可设置 0–60 秒的悬停延迟，0 表示立即隐藏。延迟输入框只在该设置打开时可用（关闭时延迟不被读取），关闭后已设置的值会保留。
- 模型窗口始终完整显示在屏幕内，同时允许覆盖任务栏、程序坞和菜单栏；被拖出屏幕的窗口会在停止拖拽后自动回到屏幕内，而不是一松手就被弹回，因此跨屏幕拖动不会被中途打断。
- 新增手柄摇杆和扳机死区设置。
- 模型管理的错误提示改为统一呈现：文件夹或封面选择窗口打不开、导入失败、模型文件夹读不出，以及改名或更换封面失败，都以同一种通知显示，不再混用页面内文字。
- 设置侧边栏改为按领域划分，不再把应用级设置全部堆在“通用”页面上。原先属于“通用”的四个分组现在各自独立成页，与通用、模型管理、快捷键、关于并列：“模型窗口”收纳模型窗口的行为、外观与帧率上限；“交互”收纳模型对行为快捷键和鼠标的响应方式；“输入”收纳按键释放超时与手柄死区；“应用”收纳运行状态、系统集成与启动更新。内容仍包含多个分组的页面会在侧边栏把它们显示为第二级，一次点击即可到达；“通用”现在只保留外观。
- 快捷键页的窗口快捷键和模型快捷键不再以页面内的选项卡切换，而是各自成为设置侧边栏第二级的独立入口：两者都能从侧边栏直接到达，页面正文按顺序展示两份列表，读屏软件因此也能在自己的作用域下逐行读出它们，而不是遇到一个页面从未对外暴露的选项卡控件。
- 快捷键页的两份列表现在各自以开关开头：窗口快捷键上方是“启用窗口快捷键”，模型的动作和表情上方是“启用模型快捷键”。开关只有标题、没有描述行，因为标题已经说清了它的作用。模型开关原先在“交互”页面，与它控制的那份列表隔着一整页，于是满屏快捷键按下去没有反应，却看不到任何解释。窗口快捷键此前根本没有开关，唯一的办法是逐个清除绑定，而清除无法撤销。两个开关都不会清除任何内容：已录入的组合键全部保留，重新打开开关即可让对应的快捷键生效，无需重新录制。两者互不影响：模型快捷键关闭时窗口快捷键照常可用，反过来也一样。开关关闭时，它所在分组的其余设置项会整体置灰并停止响应点击与键盘操作，重新打开即恢复可用。
- “开关控制下属设置项”从此遵循同一条规则：开关自身永远保持可点（否则关掉后无法再打开），而它控制的行在开关关闭或页面处于不可编辑状态时一起禁用；置灰的同时也停掉交互与键盘焦点，读屏软件同步报为不可用，而不是只变灰仍可点击。悬停隐藏延迟行与两个快捷键分组都按这条规则工作。
- 设置侧边栏的每个顶层页面（通用、模型管理、模型窗口、交互、输入、快捷键、应用、关于）都配上了各自图标，各页面一眼即可区分。
- 配置恢复提示改为显示在页面区域之上，不再放在某个页面里，因此无论当前打开哪一页它都可见——恢复之后也不例外，而它正是提示用户重启的那条信息。提示里的按钮文案也改为与它实际执行的动作一致：该按钮执行的是恢复整份配置，此前却沿用快捷键页的说法，现在使用与它本来就对外暴露的无障碍名称相同的文案。读屏软件现在也会读出提示本身的文字，此前它只能读到那个恢复按钮。
- 设置项不再把标题重复说一遍。标题已经说清用途的行不再显示第二行说明：模型窗口的显示、始终置顶、鼠标穿透三个开关，镜像、动作音效与鼠标相关各行，菜单栏、托盘与任务栏图标开关，以及主题和语言选择都删掉了只是换种说法的描述。标题无法表达的信息继续保留：数值项的取值区间、悬停延迟与按键释放兜底中 0 的含义、“保持在屏幕内”可能覆盖的系统区域、死区与行为快捷键的定义，以及“登录时启动”所有需要修复或不可用的状态。唯一带条件的行也按同一规则处理：登录开关只在确实有需要解释的内容时才显示描述，两个稳定状态因此不再多出一行。读屏软件同步调整：删除描述的行也不再把它当成控件的值重复朗读。
- 重写了需要靠说明才读得懂的标题：“Overlay”页面改为“Model window”，与窗口内其余英文文案以及中文侧边栏一贯的名称一致；“Click-through overlay”改为“Mouse click-through”；“Keep inside the screen”改为“Keep on screen”；“Mirror model”补上了水平方向；悬停延迟与按键释放超时两行把“(s)”“(ms)”写成完整的“seconds”“milliseconds”。运行状态行的值也不再重复自己的标题，由“Runtime ready - revision 1”变为“Ready · refresh 1”。

- 设置界面不再使用应用内部的术语。“指针”在所有与鼠标相关的设置里统一改为“鼠标”：交互页的分组、镜像与忽略两行，以及悬停隐藏与其延迟两行。“按键释放兜底”改为“按键释放超时”，并直接说明它的作用：在设定的时间内没有收到按键松开事件时自动松开，避免按键一直卡住。状态行不再说正在连接“运行时”，设置没生效时也不再由“运行时”来报告。更新提示改说“当前版本”而不是“此构建”，没有对应安装包时直接说“该版本没有提供适用于这台电脑的安装包”，配置详情里的“Schema”改为“配置格式”。读屏软件读到的错误与状态文案使用同一套说法。

- 模型卡片只显示封面图与模型名称：卡片原先列出的纹理、表情、动作数量已删除，只有模型包无效或正在确认删除时，卡片才会显示额外文字。

### 💻 平台变化

- 首发支持 Windows 10 1903+（x64）、macOS 12+（Intel）和 macOS 12+（Apple 芯片）。
- Windows ARM 设备通过系统 x64 仿真运行，不再提供 Windows x86 和原生 Windows ARM64 安装包。
- Linux 暂不提供首发支持。
- macOS 上的“登录时启动”需要 macOS 13 或更高版本。

### 🗑️ 移除或不再支持

- 不再自动导入旧版设置、快捷键、模型配置或已安装模型。
- 不再支持将模型直接拖入窗口导入，请改用模型库中的文件夹或 ZIP 导入。
- 移除模型卡片上的表情按钮。模型的表情和动作列在“快捷键”页面并紧邻其绑定的快捷键，不再在两处重复展示同一份列表。
- 移除繁体中文、葡萄牙语和越南语选项；当前提供跟随系统、简体中文和英语。
- Windows 的按键自动释放延迟由按秒配置调整为按毫秒配置的释放兜底超时。
- 移除设置窗口底部的工具栏。其中的刷新与退出按钮已删除：窗口会自行保持显示最新设置，退出 BongoCat 仍可通过系统菜单完成。
