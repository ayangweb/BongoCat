[package]
name = "bongocat-ui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
async-channel.workspace = true
bongocat-config.workspace = true
bongocat-i18n.workspace = true
pulldown-cmark.workspace = true
bongocat-platform.workspace = true
gpui-kit.workspace = true
raw-window-handle.workspace = true

# Headless UI rendering for the window tests: `test-support` gives GPUI a test
# platform, so a window can be opened, rendered and clicked without a display.
[dev-dependencies]
gpui-kit = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
