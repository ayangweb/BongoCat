[package]
name = "bongocat-gpui-settings-spike"
version = "0.1.0"
edition = "2024"
publish = false

# Keep this probe independent from the legacy root Tauri workspace.
[workspace]

[dependencies]
# Keep the default precompiled-shader path aligned with release packaging.
gpui = "=0.2.2"
unicode-segmentation = "=1.13.3"

[profile.release]
opt-level = 2
lto = "thin"
